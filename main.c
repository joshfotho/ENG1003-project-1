#include <stdio.h>
int main(){
    int k = 1, counter = 0; //key that values are shifted by
    int letters[26]; //array that stores letters
    //set each element of the array to ASCII value for its respective letter
    int counter; 
    for(counter = 0; counter < 26; counter++) {
        x[counter] = counter + 65; 
        }
    printf("%c", x[0]);
}